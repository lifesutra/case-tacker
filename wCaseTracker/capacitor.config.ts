import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.casetracker.app',
  appName: 'Case Tracker',
  webDir: 'dist/wCaseTracker/browser'
};

export default config;
